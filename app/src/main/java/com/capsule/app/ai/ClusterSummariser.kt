package com.capsule.app.ai

class ClusterSummariser(private val llmProvider: LlmProvider) {
    // Stub implementation to be filled in during Block 6 (T138)
    suspend fun summarise(cluster: Any): Any? = null
}
