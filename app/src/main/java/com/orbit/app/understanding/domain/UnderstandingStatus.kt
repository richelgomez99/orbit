package com.orbit.app.understanding.domain

enum class UnderstandingStatus {
    READY,
    LIMITED,
    FAILED
}

enum class EvidenceLevel {
    FULL,
    PARTIAL,
    METADATA_ONLY,
    VISUAL_ONLY
}
